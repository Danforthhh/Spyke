# Cloudflare Worker URL that proxies Anthropic API calls
# Deploy the worker: cd worker/ && npx wrangler deploy
# Then copy the displayed URL here
VITE_WORKER_URL=https://spyke.YOUR_SUBDOMAIN.workers.dev
